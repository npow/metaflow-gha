# Auto-generated by metaflow-gha — do not edit manually.
# This workflow is a thin caller that delegates to the reusable worker
# in npow/metaflow-gha. No metaflow-gha code needs to live in your repo.
name: Metaflow GHA Worker

on:
  workflow_dispatch:
    inputs:
      run_id:
        required: true
        type: string
      worker_index:
        required: true
        type: string
      s3_root:
        required: true
        type: string
      worker_ref:
        required: false
        default: "main"
        type: string

jobs:
  worker:
    uses: npow/metaflow-gha/.github/workflows/worker.yml@main
    with:
      run_id: ${{ inputs.run_id }}
      worker_index: ${{ inputs.worker_index }}
      s3_root: ${{ inputs.s3_root }}
      worker_ref: ${{ inputs.worker_ref }}
    secrets: inherit
